import NotFound from "../not-found";

// PUBLIC_INTERFACE
export default function NotFoundPage() {
  /** Static-export compatible not-found entrypoint for Next.js export worker. */
  return <NotFound />;
}
